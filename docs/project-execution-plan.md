# Retrue 项目执行计划

> 版本：V1.0  
> 状态：进行中  
> 依据：[产品设计文档 V1.0](Retrue_项目设计文档_V1.0.md)  
> 更新规则：每完成一个可验收单元，更新本计划中的状态、完成日期、风险和下一步。

## 1. 项目目标

V1 目标不是堆叠完整 CRM 功能，而是让单一康复师能稳定完成以下日常闭环：

```text
今日客户 → 课前回顾 → 训练记录 → AI 草稿确认 → 正式保存 → 时间线 → 下次计划
```

V1 成功标准：康复师可以快速找到当天客户；课后用自然语言记录；AI 正确生成可编辑草稿；确认后可追溯保存；下一次课前可看到上次情况和计划。

## 2. 范围与非范围

### V1 范围

- 登录和单康复师数据隔离。
- 客户档案、今日客户和客户时间线。
- 单客户训练记录、训练动作、疼痛、观察与下一步计划。
- AI 训练记录草稿解析与人工确认。
- 审计日志基础能力。

### V1 不做

- 小程序、客户端、多人协作、复杂 Excel 课表解析。
- RAG、LangGraph、异步任务队列、Redis、微服务。
- 自动推进康复阶段、自动扣课时或 AI 自动修改正式记录。

## 3. 阶段看板

| 阶段 | 状态 | 目标 | 关键交付物 | 验收条件 |
| --- | --- | --- | --- | --- |
| 0. 项目基线 | 已完成 | 建立可开发环境与规范 | 前后端骨架、Compose PostgreSQL、开发规范 | 本地数据库、Django 检查与前端构建通过 |
| 1. 基础架构 | 已完成 | 打通认证、统一 API 和基础数据层 | 账号、康复师、统一响应、权限基座、审计基座 | 未登录不可访问；响应符合 `{code,message,data}` |
| 2. 客户与工作台 | 已完成 | 可管理客户并查看今日工作 | 客户档案、脱敏列表、今日课程/待办基础页 | 仅能查看本人客户；列表手机号脱敏 |
| 3. 训练记录闭环 | 已完成 | 可完成一节课的正式记录 | 训练记录、动作明细、时间线、下次计划 | 保存后时间线可回顾；历史修改留审计 |
| 4. AI 草稿确认 | 已完成 | 自然语言快速生成训练记录草稿 | 原始输入、AI 草稿、确认编辑、失败与候选处理 | AI 不会直接写正式记录；确认后才保存 |
| 5. V1 联调验收 | 已完成 | 验证真实工作流可用 | 端到端场景、缺陷修复、操作说明 | 通过核心验收场景，无 P0/P1 缺陷 |
| 6. V1 后续迭代 | 进行中 | 扩展专业评估与辅助业务 | 评估、计划、动作库、家庭训练、课时、回访 | 依据优先级逐项立项与验收 |

状态取值：`待开始`、`进行中`、`阻塞`、`已完成`。每次只允许一个阶段处于“进行中”。

## 4. 执行任务清单

### 阶段 1：基础架构

- [x] 定义自定义用户或认证策略，以及康复师身份模型。
- [x] 实现统一 API 响应、全局异常处理与错误码约定。
- [x] 实现认证、登录、登出和基础权限控制。
- [x] 定义审计日志基础模型与记录服务。
- [x] 建立前端路由、布局、Axios 拦截器、Pinia 会话状态和 API 类型基础。
- [x] 维护对应 Model、migration、SQL 文件和 API 文档。

验收：无登录态无法访问业务 API；登录后只能读取自己的资源；所有接口遵循统一响应格式。

### 阶段 2：客户与今日工作台

- [x] 定义 Customer 模型、SQL 文档、迁移与 API。
- [x] 实现客户创建、编辑、搜索、状态管理和手机号脱敏。
- [x] 建立课程/日程最小模型，支持展示今日客户。
- [x] 实现客户列表、客户详情和今日工作台页面。
- [x] 加入空状态、加载状态、权限失败和表单校验。

验收：可新增客户；列表不显示完整手机号；可从今日工作台进入客户详情。

### 阶段 3：训练记录闭环

- [x] 定义 TrainingRecord、TrainingExercise、训练指标与下一步计划的数据结构。
- [x] 实现创建、查看和人工修订训练记录。
- [x] 实现客户时间线，按事件日期正确排序。
- [x] 实现修改原因、前后快照、操作人和时间的审计记录。
- [x] 完成训练记录编辑页与客户时间线 UI。

验收：训练动作、训练量、客户感受、康复师观察和下次计划可完整保存；修改历史记录可追溯。

### 阶段 4：AI 草稿确认

- [x] 定义 AI provider 抽象、Pydantic 草稿 schema 与解析服务。
- [x] 保存原始输入、AI 初始结果、确认结果和错误信息。
- [x] 实现训练记录自然语言解析为待确认草稿。
- [x] 实现前端草稿预览、编辑、确认和取消交互。
- [x] 实现客户识别不确定时的候选选择流程。
- [x] 记录 AI 调用必要审计信息，避免输出敏感内容到日志。

验收：AI 只能生成草稿；不确认不创建正式训练记录；人工编辑后保存的是最终确认结果。

### 阶段 5：V1 联调验收

- [x] 准备无真实敏感信息的演示客户与课程数据。
- [x] 按核心验收场景完成端到端测试。
- [x] 修复阻断使用的缺陷，完善错误提示和空状态。
- [x] 完成部署说明、环境变量说明和备份恢复说明。
- [ ] 与康复师进行一次真实流程试用，记录反馈并形成 V1.1 Backlog（需真人参与）。

验收：核心场景全部通过；无 P0/P1 缺陷；文档可支持新的开发者完成本地启动。

## 5. 核心验收场景

1. 康复师登录后只能看到自己的客户和记录。
2. 康复师在今日工作台找到客户，进入档案并看到上次训练与下次计划。
3. 康复师创建训练记录，填写动作、训练量、疼痛、观察和计划后正式保存。
4. 康复师输入自然语言训练描述，获得 AI 草稿；编辑并确认后形成正式记录。
5. 未确认、取消或 AI 解析失败时，不产生正式训练记录。
6. 修改已保存训练记录时必须填写原因，且时间线可查到变更历史。
7. 客户列表默认脱敏手机号；无权访问其他康复师的数据。

## 6. 依赖与风险

| 项目 | 风险或依赖 | 应对方式 |
| --- | --- | --- |
| AI 服务商 | API Key、成本、结构化输出稳定性未确定 | 先完成 provider 抽象和 mock；接入前确认服务商与预算 |
| 数据模型 | 评估、课时与课表细则仍存在未决项 | V1 只实现核心训练闭环，未决项进入后续 Backlog |
| 隐私数据 | 健康数据和手机号敏感 | 环境变量、权限隔离、脱敏、审计与测试假数据 |
| Excel 课表 | 格式定制化且尚未提供样本 | 不纳入 V1；拿到真实文件后单独设计 |
| 真实试用 | 需求可能在实际记录过程中调整 | 每阶段验收后安排短试用，反馈进入下一阶段计划 |

## 7. 进度更新模板

每次更新请在本节追加一条记录：

```md
### YYYY-MM-DD｜阶段 N

- 状态：进行中 / 已完成 / 阻塞
- 已完成：
- 进行中：
- 风险或阻塞：
- 下一步：
- 验证结果：
```

### 2026-08-26｜阶段 0

- 状态：已完成
- 已完成：前后端项目骨架、Python 虚拟环境、独立 Docker Compose PostgreSQL、初始迁移、开发规范和 Git 初始化。
- 进行中：无。
- 风险或阻塞：AI 服务商、认证方案和真实课程数据尚未确定。
- 下一步：进入阶段 1，确定认证策略与账号/康复师数据模型。
- 验证结果：Django `manage.py check`、前端 `npm run build` 与 PostgreSQL 迁移均已通过。

### 2026-08-26｜阶段 1

- 状态：已完成
- 已完成：
  - 认证方案定为 Django Session + Cookie；自定义 User 模型与康复师 Therapist 模型。
  - 统一 API 响应 `{code,message,data}`、全局异常处理与业务码约定（apps/common/）。
  - 登录 / 登出 / 当前用户接口，默认 IsAuthenticated 权限，未登录返回 401。
  - 审计日志基础模型 AuditLog、记录服务与分页/对象查询接口。
  - 前端路由守卫、桌面布局、Axios 拦截器、Pinia 会话 store、统一 API 类型。
  - 演示数据初始化命令 init_demo_data；对应 migration、SQL 文件与 API 文档。
- 进行中：补充 auth / audit / 响应工具的单元测试。
- 风险或阻塞：阶段 1 开发中发现并处理了自定义 User 模型引入导致的数据库历史不一致（已重置空的开发库重新迁移）。
- 下一步：进入阶段 2「客户与今日工作台」，定义 Customer 模型与脱敏列表。
- 验证结果：后端 `manage.py check` 通过；错误密码登录返回 400、未登录返回 401、登录后带 Cookie 访问受保护接口返回 200；前端 `npm run build` 通过；Vite 代理登录认证闭环验证通过。

### 2026-08-26｜阶段 2

- 状态：已完成
- 已完成：
  - Customer 客户档案模型（含手机号脱敏 mask_phone 与状态管理），migration 与 SQL 文档。
  - 课程/日程最小模型 CourseSession，用于今日工作台展示今日客户。
  - 客户列表（脱敏）、详情（完整手机号）、创建、更新、搜索与状态筛选接口，强制康复师数据隔离。
  - 今日课程查询与课程创建接口。
  - 前端客户列表页、客户详情页、今日工作台页面及对应 API 模块与类型。
  - 前端 Axios 拦截器支持 Session 认证下的 CSRF token 自动附加。
- 进行中：无。
- 风险或阻塞：复杂 Excel 课表解析仍不纳入 V1；当前课程为最小模型。
- 下一步：进入阶段 3「训练记录闭环」，定义 TrainingRecord/TrainingExercise 与客户时间线。
- 验证结果：后端 28 个测试全部通过；客户创建自动脱敏（138****8000）、列表不返回完整手机号、无法访问他人客户（404）、今日课程返回脱敏客户信息均验证通过；前端 `npm run build` 通过；CSRF 登录-创建-查询闭环验证通过。

### 2026-08-26｜阶段 3

- 状态：已完成
- 已完成：
  - TrainingRecord / TrainingExercise 训练记录与动作明细模型，migration 与 SQL 文档。
  - 训练记录创建、详情、分页列表、人工修订接口，客户时间线查询。
  - 修订正式记录强制填写修改原因，记录前后快照、操作人与时间到审计日志。
  - 前端训练记录编辑页（新建/修订）、客户训练时间线组件，并集成到客户详情页。
- 进行中：无。
- 风险或阻塞：训练指标暂以自由文本与动作明细承载，结构化评估指标留待后续。
- 下一步：进入阶段 4「AI 草稿确认」，定义 AI provider 抽象与 Pydantic 草稿 schema。
- 验证结果：后端 35 个测试全部通过；创建训练记录含动作、时间线倒序、修订填原因（200）、不填原因（400）、审计日志记录 update/create 均验证通过；前端 `npm run build` 通过。

### 2026-08-26｜阶段 4

- 状态：已完成
- 已完成：
  - AI provider 抽象（BaseProvider + AIProviderError）与工厂，当前使用 MockProvider（真实服务商未定）。
  - Pydantic 训练草稿 schema（TrainingDraft / ExerciseDraft）。
  - AiDraft 草稿模型保存原始输入、AI 结果、确认结果与错误信息，migration 与 SQL 文档。
  - 训练文本解析、草稿确认（创建正式记录）、取消、客户候选接口。
  - 前端 AI 草稿工作台：自然语言输入、草稿预览/编辑、客户候选选择、确认/取消。
- 进行中：无。
- 风险或阻塞：AI 服务商与预算未确定，当前为 mock 规则解析；接入真实模型后替换 MockProvider。
- 下一步：进入阶段 5「V1 联调验收」，准备演示数据与端到端测试。
- 验证结果：后端 41 个测试全部通过；草稿解析提取动作/感受/计划、确认创建正式记录、取消不创建记录、空文本返回 400、客户候选脱敏均验证通过；前端 `npm run build` 通过。

### 2026-08-26｜阶段 5

- 状态：已完成（真人试用待安排）
- 已完成：
  - 扩展 init_demo_data 命令：创建演示客户（测试客户 A/B/C）与今日/明日课程，全部为明确标记的模拟数据。
  - 按核心验收场景完成端到端测试：登录隔离、今日工作台、训练记录、AI 草稿确认、取消不落库、修订审计、脱敏与数据隔离，15/15 通过。
  - 修复开发中发现的问题（训练记录修订鉴权、AI 动作提取、CSRF 前端处理）。
  - 完成部署说明、环境变量说明与备份恢复说明（docs/deployment.md）。
- 进行中：与康复师进行真实流程试用（需真人参与）。
- 风险或阻塞：无 P0/P1 缺陷；AI 服务商未定仍使用 mock。
- 下一步：安排真人试用后形成 V1.1 Backlog。
- 验证结果：7 个核心验收场景、15 项检查全部通过；前后端联调冒烟通过；后端 41 个测试、前端构建通过。

#### V1.1 Backlog（试用后更新）
- 待真人试用后记录真实反馈（编辑体验、动作库、疼痛记录细节等）。

### 2026-08-26｜阶段 6 - Phase 2 专业评估

- 状态：进行中
- 已完成：
  - 康复计划 RehabPlan 与康复阶段 RehabStage 模型（默认四阶段：急性期/恢复期/力量重建/功能回归）。
  - 康复计划列表/创建、当前阶段查询、设置阶段接口（设置新阶段自动关闭上一阶段，AI 不自动改阶段）。
  - 评估 Assessment 与评估指标 AssessmentMetric 模型（首次评估/阶段复评；疼痛 NRS、肌力 0-5 级、活动度、特殊测试、功能动作）。
  - 评估创建/详情/更新接口，指标嵌套保存，更新记录审计。
  - migration、SQL 与 API 文档。
- 进行中：专业评估前端页面尚未实现。
- 风险或阻塞：无。
- 下一步：实现专业评估前端（客户详情页集成评估与康复阶段展示）。
- 验证结果：rehab/assessments 10 个测试通过，后端共 51 个测试通过。

### 2026-08-26｜阶段 6 - Phase 3 回访/复查

- 状态：已完成
- 已完成：
  - 专业评估前端（客户详情页集成康复概览：当前阶段、评估列表、评估编辑、阶段调整）。
  - 回访/复查 FollowUpTask 模型、接口、测试与文档。
- 进行中：Phase 3 其余子模块（动作库、家庭训练、课时）。
- 下一步：实现动作库与家庭训练。
- 验证结果：前端构建通过；专业评估与回访端到端验证通过；后端 54 个测试通过。

### 2026-08-26｜阶段 6 - Phase 3 动作库

- 状态：已完成
- 已完成：
  - 动作库 Exercise 模型（官方动作 + 个人动作）与 ExerciseAlias 别名模型。
  - 动作库列表/创建/详情/更新接口，官方动作不可修改，个人动作数据隔离。
  - migration、SQL 与 API 文档。
- 进行中：Phase 3 其余子模块（家庭训练、课时）。
- 下一步：实现家庭训练。
- 验证结果：exercises 5 个测试通过，后端共 59 个测试通过。

### 2026-08-26｜阶段 6 - Phase 3 家庭训练

- 状态：已完成
- 已完成：
  - 家庭训练计划 HomeTrainingPlan 与 HomeTrainingExercise 模型（动作、组数、次数、频率、注意事项）。
  - 家庭训练计划列表/创建/详情/更新接口，嵌套动作整体替换。
  - migration、SQL 与 API 文档。
- 进行中：Phase 3 剩余子模块（课时管理）。
- 下一步：实现课时管理。
- 验证结果：training 共 10 个测试通过，后端共 62 个测试通过。

### 2026-08-26｜阶段 6 - Phase 3 课时管理

- 状态：已完成
- 已完成：
  - 课时包 CoursePackage 与课时调整 CourseAdjustment 模型。
  - 课时消耗服务（剩余课时校验、审计）、人工调整服务（必填原因）。
  - 课时包列表/创建、课时调整接口。
  - migration、SQL 与 API 文档。
- 进行中：Phase 3 全部子模块已完成（动作库、家庭训练、课时、回访）。
- 下一步：阶段 6 补充辅助业务前端，或进入 V1.1 Backlog 立项。
- 验证结果：courses 6 个测试通过，后端共 68 个测试通过。

### 2026-08-26｜阶段 6 - Phase 3 辅助业务前端

- 状态：已完成
- 已完成：
  - 辅助业务前端组件 AuxiliaryServices（家庭训练、课时包、回访展示与操作弹窗）。
  - 回访、课时包前端 API 模块与类型；家庭训练 API 补充。
  - 客户详情页集成辅助业务区域。
- 验证结果：辅助业务端到端验证通过（家庭训练/课时包/回访创建与调整均 200）；后端 68 个测试通过；前端构建通过。

### 2026-08-27｜阶段 6 - Phase 4 备课助手

- 状态：已完成
- 已完成：
  - AI provider 增加 prepare_lesson 备课建议能力（mock 规则实现）。
  - 备课助手服务：系统汇总客户历史（上次训练、疼痛、阶段、下次计划）+ AI 建议。
  - 备课助手接口（/api/ai/prepare-lesson/），含风险提醒规则。
  - API 文档更新。
- 进行中：Phase 4 其余能力（特殊测试推荐、检查部位推荐、风险提醒、阶段进展参考、专业问答）。
- 下一步：按优先级推进 Phase 4 其余 AI 增强能力。
- 验证结果：ai 8 个测试通过，后端共 70 个测试通过。

### 2026-08-27｜阶段 6 - Phase 4 备课助手前端

- 状态：已完成
- 已完成：
  - 备课助手前端组件 LessonPreparation（客户历史回顾 + AI 备课建议 + 风险提醒展示）。
  - 备课助手 API 模块与类型，集成到客户详情页。
- 验证结果：备课助手端到端验证通过（返回阶段建议与风险提醒）；前端构建通过。

### 2026-08-27｜阶段 6 - Phase 4 风险提醒

- 状态：已完成
- 已完成：
  - RiskAlert 风险提醒模型（风险等级、发现依据、建议动作、确认状态、处理结果）。
  - 风险检测服务（连续 NRS>=6 触发高风险提醒）、风险列表/检测/确认更新接口。
  - migration、SQL 与 API 文档。
- 验证结果：ai 共 10 个测试通过，后端共 72 个测试通过。

### 2026-08-27｜阶段 6 - Phase 4 阶段进展参考

- 状态：已完成
- 已完成：
  - 阶段进展参考服务：从历史训练记录与评估指标提取疼痛/活动度变化趋势。
  - 阶段进展分析接口，AI 不自动修改康复阶段。
  - API 文档更新。
- 验证结果：ai 共 12 个测试通过，后端共 74 个测试通过。

### 2026-08-27｜阶段 6 - Phase 4 智能工作台前端

- 状态：已完成
- 已完成：
  - 智能工作台前端（原备课助手扩展）：AI 备课建议、阶段进展参考、风险提醒一体化展示。
  - 风险提醒确认处理交互；阶段进展趋势标签。
  - API 模块与类型补充。
- 验证结果：智能工作台端到端验证通过；前端构建通过。
